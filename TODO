server-side:

    - figure out how to alarm (cron?)
    - implement syntax checks
    - cancel/add command to running timer
    - flag to enable/disable notifications?
    - default config

client-side:
    
    - implement checks for unnecessary arguments
    - foreground option?
    - local option without involving the server
    - bit of code cleanup
    - simplify usage

general:

    - TEST TEST TEST